#include <cmath>
#include <iostream>
#include <fstream>
#include "Calculations.h"
using namespace std;

int main()
{
	ofstream myfile;
	myfile.open("inMeanStd.dat");
	int data = 0;
	file >> inMean
	cout << "Enter 4 numbers:";
	for (i = 0; i < 4; ++i) {
		cin >> data[i];
	f
	}

	cout << endl << "Stand Deviation = " << calculateSD(inMeanStd.dat);
	cout << endl << "Mean = " << calculateMean(inMeanStd.dat);

	return 0;
}